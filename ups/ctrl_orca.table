setupRequired(cat >= 4.6.0.0)
setupRequired(utils >= 4.6.0.0)
setupRequired(pex_policy >= 4.6.0.0)
setupRequired(pex_harness >= 4.6.0.0)
setupRequired(daf_persistence >= 4.6.0.0)
setupRequired(ctrl_events >= 4.6.0.0)
setupRequired(ctrl_provenance >= 4.6.0.0)

#
setupOptional(condor)
setupOptional(condor_glidein)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envPrepend(PATH, ${PRODUCT_DIR}/bin)
